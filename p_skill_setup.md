# /p スキル セットアップ指示書

Updated: 2026-08-24

`/p`（マイタスクのプライベートリストを番号で実行するスキル）を、ai-ops リポジトリ以外の
セッションでも使えるようにするための手順書。方法1と方法2のどちらかを実行する。

- **方法1**: claude.ai にアップロードして恒久登録する（一度やれば以後どこでも使える。推奨）
- **方法2**: 新規セッションに指示文を貼る（そのセッション限りで使える。アップロードが面倒なとき）

## 前提

`/p` は マイタスク MCP サーバー（`list_tasks` / `get_task` / `update_task` / `complete_task`）を
使う。MCP が接続されていないセッションでは動かない。`/t` が使えるセッションなら条件は同じ。

---

## 方法1 — claude.ai にアップロードして恒久登録する（本人作業・5分）

`/t` と同じ「custom skill」として登録する。以後どのセッション・どのディレクトリでも
`/` メニューに `/p` が出る。

1. `p.zip` を用意する
   - 2026-08-24 のチャットに添付済み。手元にあればそれを使う
   - 無ければ ai-ops リポジトリの `.claude/skills/p/SKILL.md` から作り直す。
     zip の中身は **`p/SKILL.md`** という構成にする（`SKILL.md` だけを直接 zip しない）
2. claude.ai を開く
3. Settings → Capabilities → Skills
4. 「Upload skill」から `p.zip` を選ぶ
5. 一覧に `p` が追加されたことを確認する
6. **新しいセッションを開いて** `/` を打ち、メニューに `/p` が出ることを確認する

> 既に登録済みの状態で再アップロードすると上書きになる。更新したいときも同じ手順でよい。

### なぜ本人作業なのか

claude.ai アカウント側のスキル登録は Claude 側から実行する手段が無い。ファイルの用意までは
Claude ができるが、アップロードだけは本人が行う必要がある。

---

## 方法2 — 新規セッションに貼る指示文

アップロードせずに、そのセッションだけで `/p` を使いたいときに使う。
新規セッションの最初のメッセージとして、**下のブロックの中身をそのまま貼る**。

貼った後はそのセッション内で `/p 296` のように使える。セッションを閉じると消えるので、
毎回貼り直しになる。恒久的に使いたいなら方法1にする。

````
以下の内容で ~/.claude/skills/p/SKILL.md を作成してください。作成したら「/p が使えるようになった」とだけ返してください。

---
name: p
description: 「マイタスク」(mytasks.yonetani32ai.workers.dev) のプライベートリストのタスクを番号で実行する。スラッシュコマンド "/p" に MYT 番号を付けて起動する（"/p 296"、"/p296"、"/p 296 293"）。番号なしの "/p" はプライベートの未完了一覧を出して番号を選んでもらう。「プライベートの296やって」「プライベート見せて」等の言い回しでも起動する。/t と同じ使い方で、対象がプライベートリストに限られる版。
---

# プライベートリスト ランナー（/p）

`/p` は `/t` と同じ使い方。**番号を打ったらそのタスクを実行する。**
違いは対象が マイタスク の **プライベートリスト（`list_id: private`）** に限られること。

- `/p 296` / `/p296` / `/p MYT-0296` → MYT-0296 を実行（確認を挟まずそのまま作業に入る）
- `/p 296 293` → 296 → 293 の順に1件ずつ実行
- `/p`（番号なし）→ プライベートの未完了一覧を出し、番号を選んでもらう
- `/p 296 見せて` → 中身を見せるだけで実行しない

`MYT-` の有無、ゼロ埋めの有無は問わない（`296` = `0296` = `MYT-0296`）。

## 使うツール

マイタスク MCP サーバーのツールを使う（ブラウザ操作は不要）。
必要なら `ToolSearch` で以下を読み込む:

```
select:mcp__34d5bb3f-82d5-4fc0-9e0d-6699c55b1caf__list_tasks,mcp__34d5bb3f-82d5-4fc0-9e0d-6699c55b1caf__get_task,mcp__34d5bb3f-82d5-4fc0-9e0d-6699c55b1caf__update_task,mcp__34d5bb3f-82d5-4fc0-9e0d-6699c55b1caf__complete_task
```

サーバー ID はセッションによって変わることがある。上記名で見つからなければ
`ToolSearch` に `mytasks task list` 等のキーワードを渡して同等のツール
（`list_tasks` / `get_task` / `update_task` / `complete_task`）を探す。
`mytasks.yonetani32ai.workers.dev` に `curl` / `WebFetch` で直接叩かない（401/403 になる）。

## Step 1 — 番号を確定する

**番号が付いている場合**（主動線）: `get_task(ref="MYT-0296")` で1件取る。
そのタスクの `list_id` が `private` でなければ、実行せずにその事実を伝えて指示を仰ぐ
（`/p` はプライベート用のコマンドなので、仕事リストのタスクは `/t` に回す）。
番号が存在しなければ、近い番号を推測せず「無い」と伝える。

**番号が付いていない場合**: 一覧を出す。

```
list_tasks(list="private", status="open", full_note=true, limit=50)
```

番号・タイトル・期限・`risk_level`・情報の充足状況を1行で並べる:

```
MYT-0296  冷蔵庫の保証書を探してドアの緩みをヤマデンに修理依頼   期限なし / B / 情報不足
MYT-0294  掃除ロボットの汚水タンクを空にする                     期限なし / B / 情報不足
```

そのうえで「どの番号をやりますか」と聞き、番号が返ってきたら Step 2 へ。
「一覧だけ見たい」場合はここで止める。

## Step 2 — 実行する

`get_task` の全項目と `readiness` を見て作業に入る。

**note が空、または `readiness.missing` があっても止まらない。**
タイトルから対応内容を推測して、AI 側でできる下準備を先に進める。例:

- 型番・保証期間・修理窓口・受付時間・料金相場を調べる
- 依頼メール / 電話で言うことの下書きを作る
- 注文候補（型番・価格・入手先）を比較して1〜2案に絞る
- 予約に必要な情報（診察券番号・希望日時候補）を整理する

推測が外れると手戻りになる要点（どのメーカーか、誰の予約か、予算はいくらか等）
**だけ**をまとめて本人に聞く。全項目の穴埋め質問はしない。

物理作業（タンクを空にする、浄水器を取り付ける）が本体のタスクは、
AI が代行できる部分（手順の確認、必要な部材、注意点、所要時間）を出して、
実行そのものは本人の作業として残す。

### 止める線（automation_priority_roadmap.md の判断ルール）

- 自動送信しない。メール・問い合わせフォーム・予約確定は**下書き止まり**にして、本人が送る
- 自動削除しない
- 注文・支払い・契約は下書き止まり
- `risk_level` が B / C のタスクは、実行の最後の一押しを必ず本人に渡す

## Step 3 — 分かったことをタスクに書き戻す

調べた内容や決まったことは `update_task` で `note` に残す。行頭見出しは決まった語を使う:

```
update_task(
  ref="MYT-0296",
  note="対応:\n...\n背景:\n...\n確認事項:\n...\n完了条件:\n...\n期限根拠:\n...\n出典:\n...",
  workflow_state="awaiting_confirmation"
)
```

- 既存の `note` がある場合は上書き前に内容を読み、消えて困る記述を残す（既存ファイルの上書きは確認後、と同じ扱い）
- 本人の確認待ちで止めたものは `workflow_state="awaiting_confirmation"`
- 作業途中なら `in_progress`

## Step 4 — 報告と完了

1件終わるごとに「やったこと / 残っていること / 本人がやる必要があること」を短く報告する。

完了扱いにするかは**必ず本人に聞く**。勝手に閉じない。
OK が出たら `complete_task(ref="MYT-0296")` を呼ぶ（繰り返し設定があれば次回分が自動生成される）。

複数番号をまとめて指定された場合も、1件ずつ Step 2〜4 を回し、完了確認はタスクごとに取る。

## 計測ログ（任意）

このリポジトリの計測ループに乗せたい場合、実行したタスクごとに
`record_ai_effect.ps1` 用の値（`-EstimatedManualMinutes` / `-ActualMinutes` /
`-QualityScore` / `-Workflow`）を報告の最後に添える。
`workflow` は `automation_priority_roadmap.md` の固定語彙から選ぶ
（新しい値を勝手に増やさない）。当てはまる語彙が無ければ記録を勧めない。
````

---

## 確認方法

セットアップ後、次のどちらかで動作確認する。

- `/p` （番号なし）→ プライベートの未完了一覧が出れば成功
- `/p 294` → MYT-0294「掃除ロボットの汚水タンクを空にする」の作業に入れば成功

## うまくいかないとき

| 症状 | 原因と対処 |
|---|---|
| `/` メニューに `/p` が出ない | スラッシュコマンドの一覧はセッション開始時に読み込まれる。スキルを追加したセッションでは出ない。**新しいセッションを開き直す** |
| `/p` と打っても反応しない | メニューに出なくても、文章として `/p 296` と打てば拾われる。それでも駄目ならスキル未登録。方法1か方法2をやり直す |
| 「マイタスクのツールが無い」と言われる | そのセッションにマイタスク MCP が接続されていない。`/t` が使えるか試し、同じく駄目なら接続設定を確認する |
| 仕事リストの番号を指定して止まる | `/p` はプライベート専用。仕事リストのタスクは `/t` を使う |

## 関連

- スキル本体: `.claude/skills/p/SKILL.md`（このリポジトリ）
- 経緯: https://github.com/yonex32-ux/ai-ops/pull/2
- 登録タスク: MYT-0298「/p スキルを claude.ai の Skills にアップロードして恒久登録する」
