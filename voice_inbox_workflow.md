# Voice Inbox Workflow（音声受信箱ワークフロー）

Updated: 2026-07-04

## Goal

ポケットや会議で録った音声メモを、業務で使える文脈だけに絞って取り込み、
その中に現れる「米谷さんの判断軸」を継続的に蓄積する。
音声は入力チャネルであって、新しい workflow カテゴリではない。
抽出結果は既存の workflow（`reply-triage` / `file-intake` /
`meeting-prep-followup` / `new-project-kickoff` / `budget-outsourcing-draft`）に流し込む。

## 正直な前提（この仕組みの限界）

1. **AI は音声そのものを聞けない。** 抽出は文字起こし（テキスト）に対して行う。
   音声受信箱には「音声＋文字起こし」を置き、文字起こしの場所を
   `voice_inbox_queue.csv` の `transcript_location` に記録する。
2. **AI はセッションをまたいで記憶しない。** 「私を理解する／判断軸を学習する」は、
   暗黙の記憶ではなく `judgment_axis_profile.md` に**書き残す**ことで実現する。
   毎セッション、その1枚を読めば文脈が復元できる状態にする。
3. **常時録音はしない。** ポケット内の常時録音は、同席者を無断で録る問題があるため推奨しない。
   意図して押して録る「プッシュ録音」を基本とする（→ 下の Recording guidance）。

## Recording guidance（ポケット録音の扱い）

- 「ポケットに入っている間も録れているか」は録音アプリ/端末の設定の問題で、AI 側では制御も確認もできない。
- 常時・受動録音は避ける。理由:
  - 同席者の無断録音になり得る（法務・信頼リスク）
  - 沈黙・雑音・私的会話まで混ざり、抽出コストと誤解が増える
- 推奨: **意図的なプッシュ録音**（自分の考え・決定・宿題を口頭でメモする）。
  - 例:「今日のCP案件、外注はA社に絞る。理由は納期。金額は要確認」
  - 会議録音は参加者に一言伝えてから（既存の `/rec` 運用に合わせる）。

## Standard flow

### Step 1 — 受信（Intake）
音声を音声受信箱に保管し、`record_voice_note.ps1` で
`voice_inbox_queue.csv` に1メモ1レコード追記する（`status = received`）。

### Step 2 — 文字起こし（Transcript）
音声を文字起こしし、テキストを Obsidian か Drive に置く。
その場所を `transcript_location` に記録する。ここまでが「AI が読める」状態。

### Step 3 — 文脈抽出（Extract only what matters）
文字起こしに対して下の抽出テンプレを実行し、**必要な文脈だけ**を取り出す。
雑談・言い淀み・私的な話は落とす。結果を `context_summary` に要約する。

### Step 4 — 振り分け（Route）
抽出結果を既存システムに流す。
- 案件文脈・状態・判断 → **Obsidian**（source of truth）
- AI が下準備できる作業 → **Linear**（`Inbox` に起票、`linear_workflow.md` に従う）
- 判断軸の手がかり → **`judgment_axis_profile.md`**（Step 5）
`routed_to` に行き先を記録し、`status = routed`。

### Step 5 — 判断軸の学習ループ（Learning loop）
文字起こしに現れた「なぜそう決めたか」を判断シグナルとして
`judgment_axis_profile.md` に追記する。ただし**推測は事実にしない**。
未確認のものは「仮説」として記録し、確認後に確定へ移す（実行時の判断ルールに準拠）。

## 抽出テンプレ（Step 3 で使う）

```text
この文字起こしから、業務で必要な文脈だけを抽出してください。次に分けてください:
1. 案件/相手/日付などの事実
2. 決めたこと（決定事項）
3. 保留・要確認（未決）
4. 次にやること（タスク候補）
5. 判断の理由・優先順位（＝判断軸の手がかり）
雑談・言い淀み・私的な話・重複は捨ててください。
不確かな点は「不確か」と明示し、勝手に断定しないでください。
```

## 捨てるもの（Do not capture）

1. 私的・センシティブな会話（家族、健康、金銭の私事）
2. 同席者を特定できる無断録音の部分
3. 業務に転用しない雑談・独り言
4. すでに Obsidian にある重複情報

## 効果測定との接続

音声起点の作業が「時短・より良い下書き・ミス予防・再利用テンプレ化」につながったら、
`record_ai_effect.ps1` で `effect_log.csv` に1件記録する。
`workflow` 列は**音声用の新語を作らず**、流し込んだ先の既存値を使う
（例: 音声メモ → 返信下書きなら `reply-triage`）。

## Weekly review

週末に:
1. 受信した音声メモ数（`received`）
2. 文脈抽出まで到達した数（`extracted` 以上）
3. 判断軸プロファイルに新規/確定した項目数
4. 抽出はしたが何にもつながらなかった数（＝録りすぎ/雑音の指標）
5. 転換率が低い録り方（常時録音・雑談メモ）は減らす
